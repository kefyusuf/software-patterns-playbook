# Tools

This directory is reserved for lightweight documentation support tools such as index generation or validation helpers.

The MVP stays Markdown-first and intentionally avoids generated tooling as a core dependency. Any future tool added here should support the documentation, not become a requirement for understanding or using the repository.
